/*package scanner;

// other way to implement
        // SCAN that gets the Response Information, and stores them in a Variable
      /*
        IResponseInfo response = helpers.analyzeResponse(baseRequestResponse);

        List<String> headers = response.getHeaders();

        if(!headers.isEmpty()){
            for (String header : headers) {  //for every Element

                String [] headerArray = header.split(":", 2);

*/
        //List<IScanIssue> issues = new ArrayList<>();
        //  int responseHeader = IExtensionHelpers.analyzeResponse(baseRequestResponse.getResponse()).getBodyOffset();
        // getUrl

 /*  private void getResponses() {

       String parameter = "";
       String cookie = "";
       URL url;
       byte[] response;
       //byte[] responses_original = new ArrayList<Byte>;
       for (IHttpRequestResponse rr : callbacks.getProxyHistory()) {
           try {
               url = helpers.analyzeRequest(rr).getUrl();
               response = rr.getResponse();

           }
       }
   }
}

*/